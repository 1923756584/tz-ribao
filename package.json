{
  "name": "tz-ribao",
  "version": "1.0.0",
  "description": "TZ日报 - 每日AI资讯聚合",
  "scripts": {
    "fetch": "node fetch-news.js",
    "build": "cd hugo && hugo --minify",
    "deploy": "npm run fetch && npm run build"
  },
  "dependencies": {
    "rss-parser": "^3.13.0",
    "turndown": "^7.1.2",
    "dotenv": "^16.3.1"
  }
}
